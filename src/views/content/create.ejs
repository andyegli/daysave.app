<%- include('../partials/header', { title: 'Create Content' }) %>

<div class="row justify-content-center">
  <div class="col-md-8">
    <div class="card shadow-sm">
      <div class="card-body">
        <h1 class="card-title mb-4">Create Content</h1>
        <form action="/content" method="POST">
          <div class="mb-3">
            <label for="title" class="form-label">Title:</label>
            <input type="text" id="title" name="title" class="form-control" required>
          </div>
          <div class="mb-3">
            <label for="body" class="form-label">Body:</label>
            <textarea id="body" name="body" class="form-control" rows="5" required></textarea>
          </div>
          <div class="d-flex">
            <button type="submit" class="btn btn-primary me-2">Submit</button>
            <a href="/content" class="btn btn-outline-secondary">Back to Content List</a>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>

<%- include('../partials/footer') %>